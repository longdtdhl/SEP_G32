@page
@model OPCBS.Web.Pages.BusinessManager.Specializations.CreateModel
@{
    ViewData["Title"] = "Add Specialization";
    ViewData["UseDashboardLayout"] = true;
}

<div class="mb-page-header d-flex align-items-center gap-3">
    <a asp-page="Index" class="btn btn-sm btn-outline-secondary"><i class="bi bi-arrow-left"></i></a>
    <h1 class="mb-0"><i class="bi bi-plus-circle me-2" style="color:var(--mb-primary);"></i>Add Specialization</h1>
</div>

@if (Model.Error != null) { <div class="alert alert-warning">@Model.Error</div> }

<div class="card border-0 shadow-sm" style="max-width:600px;">
    <div class="card-body p-4">
        <form method="post">
            <div class="mb-3">
                <label class="form-label fw-semibold">Name <span class="text-danger">*</span></label>
                <input asp-for="Input.Name" class="form-control" placeholder="e.g. Clinical Psychology, Child Therapy..." required />
            </div>
            <div class="mb-4">
                <label class="form-label fw-semibold">Description</label>
                <textarea asp-for="Input.Description" class="form-control" rows="3" placeholder="Describe this specialization..."></textarea>
            </div>
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary"><i class="bi bi-check-lg me-1"></i>Create</button>
                <a asp-page="Index" class="btn btn-outline-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>
