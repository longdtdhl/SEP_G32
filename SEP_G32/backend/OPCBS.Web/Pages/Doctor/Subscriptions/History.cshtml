@page
@model OPCBS.Web.Pages.Doctor.Subscriptions.HistoryModel
@{ ViewData["Title"] = "Subscription History"; ViewData["UseDashboardLayout"] = true; }

<div class="mb-page-header d-flex justify-content-between align-items-center">
    <h1>Subscription History</h1>
    <a asp-page="Status" class="btn btn-outline-secondary btn-sm"><i class="bi bi-arrow-left"></i> Back</a>
</div>

<div class="card border-0 shadow-sm"><div class="card-body p-0">
    <div class="table-responsive"><table class="table table-hover mb-0">
        <thead class="table-light"><tr><th>Package</th><th>Status</th><th>Start Date</th><th>Expiration</th><th>Payment</th></tr></thead>
        <tbody>
            @if (Model.History.Any())
            {
                @foreach (var s in Model.History)
                {
                    <tr>
                        <td><strong>@(s.PackageName ?? "N/A")</strong></td>
                        <td><span class="badge @(s.Status == "Active" ? "bg-success" : "bg-secondary")">@(s.Status ?? "N/A")</span></td>
                        <td>@s.StartDate.ToString("MM/dd/yyyy")</td>
                        <td>@s.EndDate.ToString("MM/dd/yyyy")</td>
                        <td>@s.AmountPaid.ToString("N0") ₫</td>
                    </tr>
                }
            }
            else
            {
                <tr><td colspan="5" class="text-center py-4 text-muted">No subscription history found</td></tr>
            }
        </tbody>
    </table></div>
</div></div>
